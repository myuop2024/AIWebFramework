<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verification Result</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    .gradient-bg {
      background: linear-gradient(135deg, #4338ca, #3b82f6);
    }
  </style>
</head>
<body class="font-sans bg-gray-100 min-h-screen flex flex-col">
  <header class="gradient-bg text-white p-4 shadow-lg">
    <div class="container mx-auto">
      <h1 class="text-2xl font-bold">CAFFE Observer Platform</h1>
      <p class="text-sm opacity-80">Identity Verification</p>
    </div>
  </header>
  
  <main class="container mx-auto flex-grow p-6 flex items-center justify-center">
    <div class="bg-white rounded-lg shadow-lg p-8 max-w-md w-full">
      <div class="text-center mb-6">
        <% if (success) { %>
          <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-green-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-green-700">Verification Successful</h2>
        <% } else { %>
          <div class="w-20 h-20 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-red-700">Verification Failed</h2>
        <% } %>
      </div>
      
      <p class="text-gray-700 text-center mb-8"><%= message %></p>
      
      <div class="border-t border-gray-200 pt-6 mt-6">
        <p class="text-sm text-gray-500 text-center">
          <% if (success) { %>
            Your identity has been successfully verified. You can now close this window and return to the CAFFE Observer Platform.
          <% } else { %>
            Please try again or contact support if you continue to experience issues with verification.
          <% } %>
        </p>
      </div>
      
      <div class="mt-8 text-center">
        <button onclick="window.close()" class="px-6 py-2 bg-indigo-600 text-white rounded-md shadow-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
          Close Window
        </button>
      </div>
    </div>
  </main>
  
  <footer class="bg-gray-800 text-white text-center p-4 text-sm">
    <p>&copy; <%= new Date().getFullYear() %> CAFFE - Citizens Action for Free & Fair Elections</p>
  </footer>
  
  <script>
    // Auto-close after 10 seconds if successful
    <% if (success) { %>
    setTimeout(() => {
      window.close();
    }, 10000);
    <% } %>
  </script>
</body>
</html>